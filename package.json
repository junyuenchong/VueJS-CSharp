{
    "name": "vuejs-csharp-root",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "prepare": "husky install",
        "lint:commit": "commitlint --edit",
        "start": "docker compose -p crudapp up -d --build",
        "docker:up": "docker compose -p crudapp up -d --build",
        "docker:start": "docker compose -p crudapp up -d",
        "docker:down": "docker compose -p crudapp down",
        "docker:logs": "docker compose -p crudapp logs -f"
    },
    "devDependencies": {
        "@commitlint/cli": "^19.0.0",
        "@commitlint/config-conventional": "^19.0.0",
        "husky": "^9.0.0"
    }
}